# TikTok Data Process Progress

最后更新：2026-07-16

## 1. 项目定位

这个仓库包含多个 TikTok 数据处理工具目录，但当前重点维护的是 `sum_daily_order` 这套订单成本日报工具。

当前目标：

- 生成 TikTok Shop 订单按日汇总报表
- 支持多文件周期对比
- 支持产品级、SKU 级、样品单维度分析
- 支持面向同事交付的 Windows `exe` 运行方式

## 2. 当前主入口与主脚本

当前实际使用的主入口：

- `main.py`

当前重点维护脚本：

- `sum_daily_order/config/cal_cost_return_jp_daily.py`
- `sum_daily_order/config/cal_cost_return_vn_daily.py`

说明：

- `main.py` 是当前打包和日常运行的统一入口。
- 日本、越南已经切到 daily 逻辑。
- 墨西哥目录和配置已预留，但日报脚本还没有正式接入。

## 3. 当前目录约定

核心运行目录：

```text
tiktok_data_process/
├── main.py
├── README.md
├── TikTokDailyReport_使用说明.txt
├── progress.md
├── docs/
│   └── skills/
│       └── tiktok_daily_report/
│           └── SKILL.md
└── sum_daily_order/
    ├── config/
    │   ├── app_config.xlsx
    │   └── cache/
    ├── data/
    │   ├── data_JP/
    │   ├── data_VN/
    │   └── data_MX/
    └── result/
```

数据目录规则：

- 日本：`data/data_JP/local`、`cross-border`、`direct`
- 越南：`data/data_VN/local`、`cross_border`
- 墨西哥：`data/data_MX/local`、`direct`

注意：

- 店铺 CSV 必须放进对应店铺目录，不能混放。
- 不同店铺使用的物流成本、寄样成本、部分成本配置不同。

## 4. 当前已实现功能

### 4.1 站点与店铺

已接入：

- 日本：本土店、跨境店、直邮店
- 越南：本土店、跨境店

已预留未接入：

- 墨西哥：本土店、直邮店

### 4.2 报表输出

当前主要输出 Sheet：

- `Daily Summary`
- `Daily Product Detail`
- `SKU Detail`
- `Product Quantity Matrix`
- `Sample Statistics`
- `Period Comparison`

当前各 Sheet 口径：

- `Daily Summary`
  - 按 `文件名 + 日期` 汇总
  - 适合看每天订单数、销量、销售额、成本

- `Daily Product Detail`
  - 上半部分：按 `文件名 + 日期 + 产品大类` 汇总
  - 下半部分：按 `文件名 + 产品大类 + 产品名称` 汇总
  - 适合同时看每天产品出单情况，以及一个文件周期内 SKU 汇总表现

- `SKU Detail`
  - 按 `文件名 + 日期 + 产品大类 + 产品名称` 保留更细明细
  - 适合排查具体 SKU / 规格

- `Product Quantity Matrix`
  - 顶部：`产品 x 文件` 销量汇总
  - 下方：`文件名 + 产品名称 + 日期` 的每日销量矩阵

- `Sample Statistics`
  - `文件名 + 产品名称 + 日期` 的每日寄样矩阵

- `Period Comparison`
  - 一个文件时：只输出该文件汇总
  - 两个及以上文件时：输出文件间对比

### 4.3 运行与打包

已实现：

- `main.py` 菜单运行
- `--site JP/VN/all`
- `--list` 查看启用店铺
- 自动生成默认 `config/app_config.xlsx`
- 支持 `exe` 同级目录相对路径运行
- 无飞书密钥时优先使用本地缓存运行

## 5. 关键业务规则

### 5.1 日期归类

- 主归类字段使用 `Paid Time`
- 没有有效支付时间的订单通常不纳入已支付统计

### 5.2 产品维度

- 配置表中使用 `产品大类` 作为产品级汇总口径
- 同一产品下不同 SKU/规格可以在 `SKU Detail` 或下半部分汇总中查看

### 5.3 成本口径

- 产品成本：按销量计算
- 寄样成本：优先读取配置；如配置空值或公式未返回，则回退为 `产品成本 + 物流成本`
- 物流成本：按订单计算，不是简单按行或按销量重复累计

### 5.4 物流分摊

当前采用整单只计一次物流的逻辑：

- 同一订单内先按 `每单物流承载数量` 算出各产品候选物流成本
- 混合产品订单整单只算一次物流
- 然后按候选物流成本比例分摊到产品维度

## 6. 已修复的关键问题

- 修正日本脚本只读日本配置，不再混入越南逻辑
- 修正路径读取，统一改成相对运行目录
- 修正汇率后金额逻辑，改为 `销售额 x 汇率`
- 修正寄样数列输出位置
- 修正多文件对比时文件名展示和地区大写命名
- 修正变化率、利润率写入为百分比格式
- 修正结果表默认居中
- 修正多文件重叠日期时的汇总口径，尽量按文件拆开统计
- 修正 `每单物流承载数量` 读取范围，需要读取到 G 列
- 修正飞书公式型寄样成本读取不到时默认为 0 的问题
- 修正 `Daily Product Detail` 恢复日期列，重新支持查看每天各产品出单情况
- 修正多个文件之间插入空行，提升可读性

## 7. 当前待办

高优先级：

- 墨西哥日报脚本正式接入
- 确认 `Period Comparison` 在 3 个及以上文件时的展示是否还要继续优化
- 进一步校验越南 SKU / 名称 / 成本映射完整性

中优先级：

- 统一 README、使用说明、代码注释，避免旧方案残留
- 明确旧入口 `app.py`、`tiktok_data_tool/`、`docs/TOOL_USAGE.md` 是否保留还是归档
- 评估是否要把飞书配置同步改成更稳定的本地版本快照机制

低优先级：

- 给打包版本加入更完整的版本发布记录
- 给结果表增加更克制的视觉样式优化

## 8. 风险与注意事项

- 报表里的“利润”“利润率”不是财务净利润，只是预估毛利
- 未扣除达人佣金、广告费、平台费、退款、售后、人工、税费
- 飞书配置若更新但缓存未刷新，可能出现“明明表里改了，程序还是旧值”的情况
- 店铺目录放错会直接导致成本口径错误
- 当前仓库里还有一些旧结构和草稿目录，不应默认当作主运行链路

## 9. 建议的下次接手顺序

1. 先看 `README.md`
2. 再看 `TikTokDailyReport_使用说明.txt`
3. 再看本文件 `progress.md`
4. 然后进入 `main.py`
5. 最后按国家看对应 daily 脚本

## 10. 备注

如果后续继续扩国家，建议保持当前模式：

- `main.py` 负责统一入口和配置
- 每个国家单独一个 daily 脚本
- 成本和目录规则继续按国家脚本独立维护

这样最符合当前项目“降低误操作、减少跨国家相互影响”的设计初心。
